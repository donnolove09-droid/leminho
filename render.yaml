services:
  - type: web
    name: garena-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: 10000
      - key: MAX_CONCURRENT
        value: 2
      - key: HARD_TIMEOUT
        value: 60
